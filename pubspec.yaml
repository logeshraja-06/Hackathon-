name: smartcrop
description: Smart Crop Demand Planner (MVP)
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.6.0
  shared_preferences: ^2.5.4
  url_launcher: ^6.3.2
  file_picker: ^10.3.10
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  fl_chart: ^1.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/images/
